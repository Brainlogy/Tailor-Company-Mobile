import 'package:http/http.dart' as http;

class ResponseHandler {
  http.Response reponse(http.Response response, Uri uri) {
    // switch (response.statusCode) {

    // }

    return response;
  }
}

ResponseHandler responseHandler = ResponseHandler();
